#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    char string[50], novaString[50];
    int cont = 0;
    printf("Digite: ");
    fgets(string, 50, stdin);
    for(int i = 0 ; string[i] != '\0' ; i++){
        if
        ++cont;
    }
    cont = cont - 2;
    for(int i = cont ; i >= 0 ; i--){
        printf("%c", string[i]);    
    } 
    return 0;
}
